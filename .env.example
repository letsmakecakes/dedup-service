# dedup-service configuration
#
# This project is configured via JSON files only:
# - config.json
# - config.docker.json
#
# Environment-variable overrides are intentionally disabled.
# Keep this file only as a reminder for deployment setup conventions.
